specVersion: "v1"
serviceId: "bookhaven"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bookhaven-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
